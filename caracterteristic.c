//
// Created by Louis Guichard on 19/11/2025.
//

#include "caracterteristic.h"
#include "tarjan.h"

//PARTIE 3 :

//Fonction qui caractérise chaques classes de la chaîne
//Doit d'abord vérifier si elle est irréductible
//Si irréductible vérifier l'absorbance de chaques sommets
//Si non irréductible la parcourir
//Sur chaque classe vérifier la transience
//Si réccurence vérifier l'absorbance

t_classe_list caracterise(t_classe_list * classes_list){
  if(partition -> nb_classes == 1){ //Vérifie si la chaîne est irréductible
    for(int i = 0, i < t_classe.taille, i++){}
  }
  else{
    for(int j = 0; j < classes_list -> nb_classes; j++){ //Parcours les classes de la chaîne
      t_classe * classes = &classes_list -> classes[j];
    }
  }
}


//Sketch
t_classe_list caracterise(t_classe_list, ){
  t_classe_list -> t_classe * classes[0].absorbance == 0; //absorbance non vérifiée donc initialisée à 0
  if(t_classe_list.nb_classes == 1){ //Vérifie si la chaîne est irréductible
    for(int i = 0; i < t_classe * classes[0]; i++){ //Parcours les sommets de la classe

    }
    if(t_classe_list -> t_classe * classes[0].taille == 1){ //Vérifie l'absorbance de la classe
      t_classe_list -> t_classe * classes[0].absorbance == 1; //l'absorbance est vérifiée
    }
  }
  else{// La chaîne n'est pas irréductible
    for(int n = 0; n < t_classe_list.nb_classes; n++){ //
      if(){}
    }
  }
}